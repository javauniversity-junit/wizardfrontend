{
    "tasks": [
        {
            "type": "che",
            "label": "run webapp",
            "command": "java --spring.config.location=file:///${CHE_PROJECTS_ROOT}/license/webapp/target/application-codeeny.properties -jar  \\\n-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5005 \\\ntarget/*.jar\n",
            "target": {
                "workingDir": "${CHE_PROJECTS_ROOT}/license/webapp",
                "component": "tools"
            }
        },
        {
            "type": "che",
            "label": "prepare database",
            "command": "/opt/rh/rh-mysql57/root/usr/bin/mysql -u root < ${CHE_PROJECTS_ROOT}/web-java-spring-petclinic/src/main/resources/db/mysql/schema.sql &&\necho -e \"\\e[32mDone.\\e[0m Database petclinic was configured!\"\n",
            "target": {
                "component": "mysql"
            }
        },
        {
            "type": "che",
            "label": "maven build",
            "command": "mvn clean package -f pom-dev.xml",
            "target": {
                "workingDir": "${CHE_PROJECTS_ROOT}/license/webapp",
                "component": "tools"
            }
        }
    ]
}
